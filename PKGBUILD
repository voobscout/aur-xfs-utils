# $Id: pkgbuild-mode.el,v 1.23 2007/10/20 16:02:14 juergen Exp $
# Maintainer: Voob of Doom <vod@agrajag.0.njk.li>
pkgname=xorg-xfs-utils
pkgver=1.0
pkgrel=1
# epoch=
pkgdesc="X Font Server and friends metapackage"
arch=('i686' 'x86_64')
url="https://github.com/voobscout/aur-xfs-utils"
license=('GPL2')
# groups=()
depends=('xorg-xfs'
         'xorg-xfstt'
         'xorg-xfs-xfsinfo'
         'xorg-xfs-showfont'
         'xorg-xfs-fstobdf'
         'xorg-xfs-fslsfonts'
        )
# makedepends=()
# checkdepends=()
# optdepends=()
provides=('xorg-xfs-utils')
conflicts=('xorg-xfs-utils')
# replaces=()
# backup=()
# options=()
# install=
# changelog=
# source=('git@github.com:voobscout/aur-xfs-utils.git')
# noextract=()
# md5sums=('SKIP')

build() {
  msg2 "This is a metapackage. Nothing will be built."
}

package() {
  msg2 "This is a metapackage. Nothing will be installed."
}

# vim:set ts=2 sw=2 et:
